export function LibBasicCalculator()
{
    return("test");
}

/*{
    return("test");
}/*

/*export function LibBasicCalculator(x = 0, y = 0, op)
{
    if (op == "+")
    {
        return X + y;
    }
    if (op == "-")
    {
        return X - y;
    }
    if (op == "*")
    {
        return X - y;
    }
    if (op == "/")
    {
            if (y == 0)
                {
                    return "Error: division by zero.";
                }
            else 
                {
                    return x / y;
                }
    }
}*/